[build]
  publish = "dist"
  command = "npm run build"

[dev]
  framework = "vite"
  targetPort = 5173

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
